# Video Analysis — V2 Evidence

## 분석 대상과 방법

- 파일: `reference/warehouse_reference.mp4`
- OpenCV 측정: 1280×720, 24 FPS, 918 frames, 38.25초, 화면비 16:9
- 직접 확인한 시점: 0.00, 3.83, 9.54, 15.29, 21.00, 26.75, 32.46, 38.21초
- 구현 Evidence: `evidence/v2_reference_layout.png`

여러 시점의 원본 프레임을 추출해 정적 배치와 시간에 따라 위치가 바뀌는 요소를 구분했다. 아래의 좌표와 개수는 시각적 재구성을 위한 근사 측정이며 원본 시스템의 내부 의미를 뜻하지 않는다.

## 영상에서 직접 확인한 특징

### 전체 화면과 배경

- 전체 영상은 16:9이며 상하에 검은 영역이 일부 보인다.
- 화면 대부분은 중간 회색 바탕이고, 중앙에 약 765×615px의 밝은 흰색/미색 시설 보드가 있다.
- 시설 보드는 가로보다 세로 비중이 큰 직사각형으로 화면 중앙에 배치된다.
- 큰 Grid cell을 색칠한 형태가 아니라 얇은 선과 작은 설비가 넓은 평면에 놓인 관제형 화면이다.

### 상단 영역

- 흰 보드 상단에 파랑/청록 계열의 작은 가로 직사각 블록 8개가 반복된다.
- 최상단 중앙에는 좁게 돌출된 선 구조와 검은 원형 Marker 4개가 보인다.
- 상단 블록과 중앙 영역 사이를 옅은 적색 직교선이 연결한다.

### 중앙 반복 설비

- 가장 뚜렷한 반복 구조는 6열×3행, 총 18개의 세로형 설비 블록이다.
- 각 블록은 대략 30×60px로, 파랑 또는 청록 몸체와 밝은 보조 띠, 붉은 세로선이 조합된다.
- 열 간격은 약 95px, 행 간격은 약 122px로 비교적 규칙적이다.
- 일부 객체가 설비 가까이에 정지하거나 통과하지만 정확한 상호작용 의미는 영상만으로 확정할 수 없다.

### 좌측과 우측 영역

- 좌우 외곽에는 세로로 묶인 작은 녹색 직사각 Marker 군이 있다.
- 각 측면에는 위/아래 두 묶음과 사각 우회 Network가 보인다.
- 움직이는 초록 객체가 좌우 외곽 및 중앙 Network 사이를 오간다.

### 하단 영역

- 하단은 수평선 여러 개와 촘촘한 수직선이 결합된 직교 Network가 강조된다.
- 좌우 끝에는 아래로 내려갔다가 돌아오는 사각 loop 형태가 있다.
- 황갈색 작은 객체가 흰 보드 하단 또는 보드 바로 아래에서 여러 개 움직인다.

### 이동 Network

- 색상은 낮은 채도의 연한 적색/분홍색이다.
- 두께는 원본 해상도에서 대체로 1px 수준이며 주요 하단선만 약간 강조되어 보인다.
- 대부분 수평/수직 직교 구조이고, 중앙 설비 열 사이와 상단·측면·하단을 연결한다.
- 이 선을 V2에서는 시각 데이터인 `NetworkSegment`로만 모델링한다. 실제 route graph와 routing은 V3 범위다.

### 작은 객체의 색과 형태

- 초록색: 작은 정사각/직사각 형태
- 황갈색: 작은 사각 또는 기울어진 사각 형태
- 검정색: 작은 원형 형태
- 청록/파랑: 작은 가로·세로 직사각 형태
- 중앙 설비에 비해 이동 객체의 크기는 매우 작다.

## 시간에 따른 변화

정적인 요소:

- 흰 시설 보드와 회색 외곽
- 6×3 중앙 반복 설비
- 상단 블록과 측면 Marker 군
- 옅은 적색 Network

움직이는 요소:

- 초록 사각 객체가 중앙, 좌우, 하단 Network의 서로 다른 지점으로 이동한다.
- 검은 원형 객체가 최상단 Marker 위치와 중앙/우측의 선 위 사이에서 관찰된다.
- 황갈색 작은 객체가 중앙 하단과 화면 아래 경계 부근에서 위치를 바꾼다.
- 청록/파랑 직사각 객체도 일부 프레임에서 설비 사이 또는 우측에 위치가 달라진다.

V2는 여러 형태의 작은 중립 Entity를 배치하여 시각 언어를 재현하지만, route 기반 연속 이동은 V3에서 구현한다.

## 기존 문서에서 새로 확인하거나 구체화한 내용

- 영상의 정확한 메타데이터(1280×720, 24 FPS, 38.25초)를 확인했다.
- 중앙 반복 설비를 6열×3행, 총 18개로 확인했다.
- 상단 가로 블록을 8개, 최상단 검은 원형 Marker를 4개로 확인했다.
- 시설 보드의 근사 범위를 `(210, 50) ~ (975, 665)`로 측정했다.
- Network가 직교선 중심이며 하단에 더 조밀하다는 점을 구체화했다.
- 여러 시점 비교로 색상별 작은 객체가 정적 장식이 아니라 위치가 변하는 요소임을 확인했다.

## 구현을 위한 추정과 제한

- `FacilityLayout`, `MachineBlock`, `Station`, `NetworkSegment`, `MobileEntity`는 재현을 위한 중립 모델이다.
- 초록/검정/황갈색/파랑 객체의 산업적 역할은 확정하지 않는다.
- 원본에서 객체가 따르는 정확한 lane topology, 속도, 우선순위와 작업 규칙은 확인되지 않았다.
- V2 Evidence는 정적 대표 장면이며 원본 프레임의 특정 한 시점을 pixel-perfect하게 복사한 것은 아니다.

## Human Visual Verification

`evidence/v2_reference_layout.png`와 원본 영상을 나란히 비교하여 다음을 확인해야 한다.

- 흰 시설 보드의 크기와 화면 내 비율
- 6×3 중앙 반복 설비의 간격과 크기
- Network 선의 밝기와 두께
- 상단/좌우/하단 구역의 상대적 밀도
- 작은 객체의 크기, 색감과 화면 전체 균형
